class_name WeaponConstants

# specified how many upgrades from each tier you can get
const upgrade_tier_amount: Array[int] = [3, 2, 1, 1, 1]

# weights for each upgrade tier
const upgrade_tier_weight: Array[int] = [150, 100, 50, 30, 10]
